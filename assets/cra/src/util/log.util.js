const log = (

    (tag, what) => {
        // eslint-disable-next-line no-console
        console.log(what);
        return what;
    }

);


// noinspection JSUnusedGlobalSymbols
export default log;
